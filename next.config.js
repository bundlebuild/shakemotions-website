/** @type {import('next').NextConfig} */
module.exports = {
  // Add the following lines to your existing config or create a new config object
  output: {
    // Set the output directory to 'out' for static HTML export
    dir: 'out',
  },
};
